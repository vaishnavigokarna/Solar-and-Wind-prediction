# Frontend

React/Vite frontend for the Solar and Wind Prediction platform. It provides authentication, project/site management, site comparison, ML prediction, analytics, recommendations and reporting pages.

Run:

```powershell
npm install
npm run dev
```

Website: `http://localhost:5173`
